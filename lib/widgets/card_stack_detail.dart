import 'package:flutter/material.dart';

class CardStackDetail extends StatelessWidget {
  const CardStackDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Vengo de Card Stack");
  }
}
